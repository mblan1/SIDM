<ui:FluentWindow x:Class="SIDM.App.Views.FilePropertiesDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="Properties"
                 Height="520"
                 Width="560"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 Icon="pack://application:,,,/Resources/sidm.png"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Properties" />

        <Grid Grid.Row="1" Margin="24,8,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header card: big icon + filename + size + type -->
            <Border Grid.Row="0" Background="#15FFFFFF" CornerRadius="8" Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image x:Name="HeaderIcon" Grid.Column="0"
                           Width="40" Height="40"
                           Margin="0,0,14,0"
                           VerticalAlignment="Center" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="FileNameText" FontWeight="SemiBold" FontSize="14"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock x:Name="HeaderSubText" Opacity="0.7" FontSize="12"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Properties grid -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Type"     Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TypeText"     Margin="0,6,0,6" TextWrapping="Wrap" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Size"     Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="SizeText"     Margin="0,6,0,6" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Status"   Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="StatusText"   Margin="0,6,0,6" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Location" Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="LocationText" Margin="0,6,0,6" TextWrapping="Wrap" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Source URL" Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="UrlText"  Margin="0,6,0,6" TextWrapping="Wrap" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Created"  Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="5" Grid.Column="1" x:Name="CreatedText"  Margin="0,6,0,6" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Completed" Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="6" Grid.Column="1" x:Name="CompletedText" Margin="0,6,0,6" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Duration" Opacity="0.65" Margin="0,6,8,6" />
                    <TextBlock Grid.Row="7" Grid.Column="1" x:Name="DurationText" Margin="0,6,0,6" />
                </Grid>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <ui:Button Content="Open file" Margin="0,0,8,0"
                           Icon="{ui:SymbolIcon Symbol=Open24}"
                           Click="OnOpenFile" />
                <ui:Button Content="Open folder" Margin="0,0,8,0"
                           Icon="{ui:SymbolIcon Symbol=FolderOpen24}"
                           Click="OnOpenFolder" />
                <ui:Button Content="Copy URL" Margin="0,0,8,0"
                           Icon="{ui:SymbolIcon Symbol=Copy24}"
                           Click="OnCopyUrl" />
                <ui:Button Content="Close" Appearance="Primary" Click="OnClose" IsCancel="True" IsDefault="True" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
